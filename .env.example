# Database (PostgreSQL)
database_url=postgresql+asyncpg://user:password@host:5432/focuspulse

# Security - CHANGE THIS in production
sync_secret_key=change-me-in-production

# Optional: UTC hour for daily aggregation (default 2)
# daily_summary_hour=2
